# 飞书 CLI（lark-cli）权限申请单

> 生成时间：{{DATE}}  
> 应用名称：{{APP_NAME}}  
> App ID：{{APP_ID}}  
> 平台：{{BRAND}}

## 一、申请说明

本申请为**一次性全量申请**飞书开放平台权限，覆盖全部业务域（云文档、云盘、表格、多维表格、日历、消息、任务、知识库、邮箱、审批、会议、妙记、OKR、通讯录、白板、应用平台等），共 **{{TOTAL_SCOPES}}** 项权限，**包含全部敏感/高风险权限**。

> 已确认接受相关风险。采用一次性全量开通方式，避免后续新增权限时反复打扰审批。

## 二、需要管理员操作

请在飞书开发者后台打开该应用，进入 **权限管理** 页面，**全选下方权限清单并提交审批/启用**：

- 控制台链接：{{CONSOLE_URL}}

审批通过后，应用侧即完成全部权限开通。使用者再执行一次登录授权即可直接使用（见第四节）。

## 三、权限明细与当前状态

### 3.1 域汇总

目标权限总数：**{{TOTAL_SCOPES}}** ｜ 应用已启用：**{{ENABLED_COUNT}}** ｜ 用户已授权：**{{GRANTED_COUNT}}** ｜ 待管理员开启：**{{ADMIN_MISSING}}** ｜ 应用已启用待用户授权：**{{USER_MISSING}}**

| 域 | 说明 | 目标权限数 | 应用已启用 | 用户已授权 |
|---|---|---|---|---|
{{DOMAIN_SUMMARY_TABLE}}

### 3.2 全部权限清单（{{TOTAL_SCOPES}} 项）

状态说明：

- `已授权生效`：权限已启用且用户已完成授权，可直接使用
- `应用已启用,待用户授权`：管理员已开启，使用者扫码授权后即可生效
- `待管理员在控制台开启`：需要管理员在本单第二节的链接中开启

| 权限 | 所属域 | 状态 |
|---|---|---|
{{SCOPE_TABLE}}

## 四、审批通过后使用者的操作

1. 在本机重新运行安装脚本，或直接执行登录授权（一次即可）：

   ```bash
   lark-cli auth login --domain all
   ```

2. 验证可用性：

   ```bash
   lark-cli doctor
   lark-cli calendar +agenda
   lark-cli im +chat-list
   ```

3. 若之后权限仍有缺失，重新运行 `setup-lark-cli.ps1`，脚本会自动把缺口并入新报告。

## 五、可直接发给审批人的申请话术

您好，申请为应用「{{APP_NAME}}」（AppId：{{APP_ID}}）一次性开通飞书开放平台全部权限（共 {{TOTAL_SCOPES}} 项，覆盖所有业务域，包含敏感权限）。

用途：本地自动化 / Codex 通过官方 lark-cli 访问飞书资源。采用一次性全量开通方式，避免后续新增权限时反复申请。

控制台链接：{{CONSOLE_URL}}

已确认接受相应风险。麻烦审批，谢谢！
